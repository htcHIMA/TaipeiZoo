<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.Warehousing.Standard.ProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateTint">@color/pink_1</item>
    </style>

    <style name="Widget.Warehousing.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/brown_1</item>
        <item name="android:theme">@style/Theme.AppCompat</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Dark</item>
        <item name="android:textSize">20sp</item>
    </style>


</resources>
